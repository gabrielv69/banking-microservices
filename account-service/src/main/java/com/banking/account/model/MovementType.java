package com.banking.account.model;

/**
 * Enum representing valid movement types for banking transactions.
 * CREDIT adds to the account balance (deposit).
 * DEBIT subtracts from the account balance (withdrawal).
 */
public enum MovementType {
    CREDIT,
    DEBIT
}
